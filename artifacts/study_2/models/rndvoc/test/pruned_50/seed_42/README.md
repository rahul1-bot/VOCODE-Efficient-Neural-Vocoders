# RNDVoC — pruned_50 — Seed 42

This execution evaluates RNDVoC with the `pruned_50` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 42. It measured PESQ 3.687412 and real-time factor 0.001329 on the registered B200 lane. The 520 batch-one timing observations had p50 8.224 ms and p95 12.775 ms. Peak host-process resident memory was 8731.4 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
