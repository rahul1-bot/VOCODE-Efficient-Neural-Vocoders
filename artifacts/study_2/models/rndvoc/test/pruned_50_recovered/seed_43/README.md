# RNDVoC — pruned_50_recovered — Seed 43

This execution evaluates RNDVoC with the `pruned_50_recovered` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 43. It measured PESQ 3.872111 and real-time factor 0.001422 on the registered B200 lane. The 520 batch-one timing observations had p50 8.343 ms and p95 14.144 ms. Peak host-process resident memory was 8707.7 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
