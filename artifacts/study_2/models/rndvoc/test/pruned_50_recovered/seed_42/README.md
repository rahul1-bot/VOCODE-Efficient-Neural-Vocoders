# RNDVoC — pruned_50_recovered — Seed 42

This execution evaluates RNDVoC with the `pruned_50_recovered` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 42. It measured PESQ 3.872111 and real-time factor 0.001768 on the registered B200 lane. The 520 batch-one timing observations had p50 10.523 ms and p95 17.592 ms. Peak host-process resident memory was 8756.1 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
