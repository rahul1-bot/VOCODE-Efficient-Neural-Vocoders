# RNDVoC — baseline_cpu — Seed 43

This execution evaluates RNDVoC with the `baseline_cpu` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 43. It measured PESQ 3.892665 and real-time factor 0.059853 on the registered CPU lane. The 520 batch-one timing observations had p50 400.624 ms and p95 595.807 ms. Peak host-process resident memory was 11546.2 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
