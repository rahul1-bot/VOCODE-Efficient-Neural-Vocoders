# RNDVoC — baseline_cpu — Seed 42

This execution evaluates RNDVoC with the `baseline_cpu` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 42. It measured PESQ 3.892666 and real-time factor 0.074018 on the registered CPU lane. The 520 batch-one timing observations had p50 493.095 ms and p95 732.493 ms. Peak host-process resident memory was 11333.0 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
