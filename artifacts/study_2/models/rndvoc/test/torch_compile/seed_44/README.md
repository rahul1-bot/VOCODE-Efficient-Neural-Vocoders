# RNDVoC — torch_compile — Seed 44

This execution evaluates RNDVoC with the `torch_compile` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 44. It measured PESQ 3.893426 and real-time factor 0.002175 on the registered B200 lane. The 520 batch-one timing observations had p50 9.868 ms and p95 44.643 ms. Peak host-process resident memory was 8458.7 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
