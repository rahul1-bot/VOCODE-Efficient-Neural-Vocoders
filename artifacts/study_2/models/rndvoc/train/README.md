# RNDVoC — Train

This directory contains the recovery-training records used by reported RNDVoC interventions. The represented variants are `pruned_50_recovered`. Each variant directory contains its training configuration, execution log, and source-ordered metric history.
