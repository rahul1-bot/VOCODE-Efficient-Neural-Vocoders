# HiFi-GAN V2 — onnx_fp32 — Seed 44

This execution evaluates HiFi-GAN V2 with the `onnx_fp32` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 44. It measured PESQ 2.454959 and real-time factor 0.016190 on the registered CPU lane. The 520 batch-one timing observations had p50 109.280 ms and p95 155.346 ms. Peak host-process resident memory was 10980.2 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
