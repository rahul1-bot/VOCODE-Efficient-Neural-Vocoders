# HiFi-GAN V2 — pruned_50 — Seed 44

This execution evaluates HiFi-GAN V2 with the `pruned_50` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 44. It measured PESQ 1.881711 and real-time factor 0.001008 on the registered B200 lane. The 520 batch-one timing observations had p50 6.208 ms and p95 8.191 ms. Peak host-process resident memory was 8745.8 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
