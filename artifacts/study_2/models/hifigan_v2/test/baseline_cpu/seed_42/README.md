# HiFi-GAN V2 — baseline_cpu — Seed 42

This execution evaluates HiFi-GAN V2 with the `baseline_cpu` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 42. It measured PESQ 2.455145 and real-time factor 0.025297 on the registered CPU lane. The 520 batch-one timing observations had p50 169.918 ms and p95 229.740 ms. Peak host-process resident memory was 9791.6 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
