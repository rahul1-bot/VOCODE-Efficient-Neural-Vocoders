# HiFi-GAN V2 — fp16_weights — Seed 43

This execution evaluates HiFi-GAN V2 with the `fp16_weights` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 43. It measured PESQ 2.456130 and real-time factor 0.000792 on the registered B200 lane. The 520 batch-one timing observations had p50 4.347 ms and p95 8.777 ms. Peak host-process resident memory was 8849.8 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
