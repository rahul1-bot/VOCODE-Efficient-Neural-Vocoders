# BigVGAN — baseline_b200 — Seed 42

This execution evaluates BigVGAN with the `baseline_b200` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 42. It measured PESQ 3.320459 and real-time factor 0.005454 on the registered B200 lane. The 520 batch-one timing observations had p50 34.820 ms and p95 51.370 ms. Peak host-process resident memory was 7058.0 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
