# BigVGAN — pruned_50_recovered — Seed 43

This execution evaluates BigVGAN with the `pruned_50_recovered` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 43. It measured PESQ 3.336691 and real-time factor 0.005848 on the registered B200 lane. The 520 batch-one timing observations had p50 37.811 ms and p95 52.927 ms. Peak host-process resident memory was 8775.1 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
