# BigVGAN — pruned_30 — Seed 44

This execution evaluates BigVGAN with the `pruned_30` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 44. It measured PESQ 3.172053 and real-time factor 0.006569 on the registered B200 lane. The 520 batch-one timing observations had p50 40.597 ms and p95 77.679 ms. Peak host-process resident memory was 8790.5 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
