# BigVGAN — pruned_30 — Seed 43

This execution evaluates BigVGAN with the `pruned_30` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 43. It measured PESQ 3.172123 and real-time factor 0.005262 on the registered B200 lane. The 520 batch-one timing observations had p50 35.334 ms and p95 48.536 ms. Peak host-process resident memory was 8667.2 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
