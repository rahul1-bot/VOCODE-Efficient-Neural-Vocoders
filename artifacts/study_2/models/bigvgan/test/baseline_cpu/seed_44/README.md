# BigVGAN — baseline_cpu — Seed 44

This execution evaluates BigVGAN with the `baseline_cpu` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 44. It measured PESQ 3.320796 and real-time factor 0.399938 on the registered CPU lane. The 520 batch-one timing observations had p50 2467.026 ms and p95 4519.878 ms. Peak host-process resident memory was 14681.9 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
