# BigVGAN — pruned_50 — Seed 44

This execution evaluates BigVGAN with the `pruned_50` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 44. It measured PESQ 2.171306 and real-time factor 0.005495 on the registered B200 lane. The 520 batch-one timing observations had p50 36.788 ms and p95 48.912 ms. Peak host-process resident memory was 8784.5 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
