# BigVGAN — pruned_50 — Seed 42

This execution evaluates BigVGAN with the `pruned_50` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 42. It measured PESQ 2.171295 and real-time factor 0.005246 on the registered B200 lane. The 520 batch-one timing observations had p50 35.475 ms and p95 48.488 ms. Peak host-process resident memory was 8659.6 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
