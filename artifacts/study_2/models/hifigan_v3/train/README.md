# HiFi-GAN V3 — Train

This directory contains the recovery-training records used by reported HiFi-GAN V3 interventions. The represented variants are `pruned_50_recovered`. Each variant directory contains its training configuration, execution log, and source-ordered metric history.
