# HiFi-GAN V3 — pruned_50_recovered — Seed 42

This execution evaluates HiFi-GAN V3 with the `pruned_50_recovered` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 42. It measured PESQ 2.591053 and real-time factor 0.000435 on the registered B200 lane. The 520 batch-one timing observations had p50 2.653 ms and p95 3.435 ms. Peak host-process resident memory was 8827.0 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
