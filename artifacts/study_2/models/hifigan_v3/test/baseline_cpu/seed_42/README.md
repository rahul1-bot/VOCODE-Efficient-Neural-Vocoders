# HiFi-GAN V3 — baseline_cpu — Seed 42

This execution evaluates HiFi-GAN V3 with the `baseline_cpu` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 42. It measured PESQ 2.577035 and real-time factor 0.012274 on the registered CPU lane. The 520 batch-one timing observations had p50 82.463 ms and p95 115.631 ms. Peak host-process resident memory was 13963.1 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
