# HiFi-GAN V3 — baseline_cpu — Seed 44

This execution evaluates HiFi-GAN V3 with the `baseline_cpu` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 44. It measured PESQ 2.577034 and real-time factor 0.027152 on the registered CPU lane. The 520 batch-one timing observations had p50 176.033 ms and p95 295.158 ms. Peak host-process resident memory was 12641.4 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
