# HiFi-GAN V3 — onnx_int8_static — Seed 44

This execution evaluates HiFi-GAN V3 with the `onnx_int8_static` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 44. It measured PESQ 1.831278 and real-time factor 0.031708 on the registered CPU lane. The 520 batch-one timing observations had p50 216.819 ms and p95 302.031 ms. Peak host-process resident memory was 15843.4 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
