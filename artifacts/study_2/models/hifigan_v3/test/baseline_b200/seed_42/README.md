# HiFi-GAN V3 — baseline_b200 — Seed 42

This execution evaluates HiFi-GAN V3 with the `baseline_b200` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 42. It measured PESQ 2.576814 and real-time factor 0.000568 on the registered B200 lane. The 520 batch-one timing observations had p50 2.578 ms and p95 9.746 ms. Peak host-process resident memory was 7299.1 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
