# Vocos — int8_dynamic — Seed 44

This execution evaluates Vocos with the `int8_dynamic` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 44. It measured PESQ 3.563124 and real-time factor 0.005523 on the registered CPU lane. The 520 batch-one timing observations had p50 37.044 ms and p95 48.597 ms. Peak host-process resident memory was 9743.1 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
