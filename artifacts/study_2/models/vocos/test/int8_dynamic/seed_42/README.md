# Vocos — int8_dynamic — Seed 42

This execution evaluates Vocos with the `int8_dynamic` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 42. It measured PESQ 3.563124 and real-time factor 0.006675 on the registered CPU lane. The 520 batch-one timing observations had p50 45.158 ms and p95 58.523 ms. Peak host-process resident memory was 9676.6 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
