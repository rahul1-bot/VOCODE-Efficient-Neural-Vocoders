# Vocos — int8_dynamic — Seed 43

This execution evaluates Vocos with the `int8_dynamic` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 43. It measured PESQ 3.563132 and real-time factor 0.004447 on the registered CPU lane. The 520 batch-one timing observations had p50 29.979 ms and p95 37.439 ms. Peak host-process resident memory was 9586.2 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
