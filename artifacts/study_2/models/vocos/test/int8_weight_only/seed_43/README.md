# Vocos — int8_weight_only — Seed 43

This execution evaluates Vocos with the `int8_weight_only` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 43. It measured PESQ 3.611810 and real-time factor 0.000297 on the registered B200 lane. The 520 batch-one timing observations had p50 1.736 ms and p95 2.685 ms. Peak host-process resident memory was 6803.0 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
