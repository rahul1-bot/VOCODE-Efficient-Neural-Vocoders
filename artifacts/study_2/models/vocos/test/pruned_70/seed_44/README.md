# Vocos — pruned_70 — Seed 44

This execution evaluates Vocos with the `pruned_70` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 44. It measured PESQ 1.307128 and real-time factor 0.000404 on the registered B200 lane. The 520 batch-one timing observations had p50 2.290 ms and p95 4.702 ms. Peak host-process resident memory was 8368.8 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
