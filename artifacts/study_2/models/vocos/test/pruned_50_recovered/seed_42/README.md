# Vocos — pruned_50_recovered — Seed 42

This execution evaluates Vocos with the `pruned_50_recovered` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 42. It measured PESQ 3.134346 and real-time factor 0.000393 on the registered B200 lane. The 520 batch-one timing observations had p50 2.133 ms and p95 4.672 ms. Peak host-process resident memory was 8369.8 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
