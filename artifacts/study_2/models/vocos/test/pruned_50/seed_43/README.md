# Vocos — pruned_50 — Seed 43

This execution evaluates Vocos with the `pruned_50` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 43. It measured PESQ 1.465455 and real-time factor 0.000391 on the registered B200 lane. The 520 batch-one timing observations had p50 2.337 ms and p95 3.540 ms. Peak host-process resident memory was 8376.3 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
