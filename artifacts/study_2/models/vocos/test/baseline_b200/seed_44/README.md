# Vocos — baseline_b200 — Seed 44

This execution evaluates Vocos with the `baseline_b200` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 44. It measured PESQ 3.609623 and real-time factor 0.000344 on the registered B200 lane. The 520 batch-one timing observations had p50 1.898 ms and p95 4.182 ms. Peak host-process resident memory was 6754.1 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
