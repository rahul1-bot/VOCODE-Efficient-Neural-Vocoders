# MelGAN — baseline_cpu — Seed 44

This execution evaluates MelGAN with the `baseline_cpu` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 44. It measured PESQ 1.043047 and real-time factor 0.025941 on the registered CPU lane. The 520 batch-one timing observations had p50 175.326 ms and p95 247.611 ms. Peak host-process resident memory was 11855.2 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
