# MelGAN — pruned_50_recovered — Seed 44

This execution evaluates MelGAN with the `pruned_50_recovered` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 44. It measured PESQ 1.036193 and real-time factor 0.000554 on the registered B200 lane. The 520 batch-one timing observations had p50 2.976 ms and p95 6.234 ms. Peak host-process resident memory was 8629.2 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
