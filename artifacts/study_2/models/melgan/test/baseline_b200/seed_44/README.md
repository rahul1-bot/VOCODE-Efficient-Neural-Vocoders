# MelGAN — baseline_b200 — Seed 44

This execution evaluates MelGAN with the `baseline_b200` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 44. It measured PESQ 1.043212 and real-time factor 0.000591 on the registered B200 lane. The 520 batch-one timing observations had p50 3.416 ms and p95 6.069 ms. Peak host-process resident memory was 7169.6 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
