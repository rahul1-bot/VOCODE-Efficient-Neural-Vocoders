# MelGAN — pruned_50 — Seed 43

This execution evaluates MelGAN with the `pruned_50` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 43. It measured PESQ 1.043532 and real-time factor 0.000659 on the registered B200 lane. The 520 batch-one timing observations had p50 3.264 ms and p95 9.722 ms. Peak host-process resident memory was 8689.4 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
