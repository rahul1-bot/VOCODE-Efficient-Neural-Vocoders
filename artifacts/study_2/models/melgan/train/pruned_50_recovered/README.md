# MelGAN — pruned_50_recovered Training

This directory records the completed seed-1234 recovery training used by the admitted `pruned_50_recovered` evaluation. The run completed 13 epochs under the registered training-aware intervention.

`config.yaml` defines the training and pruning conditions, `execution.log` records the complete execution sequence, and `metrics.csv` preserves the source-ordered training and validation history.
