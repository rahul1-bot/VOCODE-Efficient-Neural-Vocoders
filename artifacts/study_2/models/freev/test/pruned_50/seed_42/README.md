# FreeV — pruned_50 — Seed 42

This execution evaluates FreeV with the `pruned_50` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 42. It measured PESQ 2.099857 and real-time factor 0.000492 on the registered B200 lane. The 520 batch-one timing observations had p50 2.881 ms and p95 5.076 ms. Peak host-process resident memory was 8614.9 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
