# FreeV — pruned_50 — Seed 44

This execution evaluates FreeV with the `pruned_50` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 44. It measured PESQ 2.099930 and real-time factor 0.000373 on the registered B200 lane. The 520 batch-one timing observations had p50 2.316 ms and p95 3.056 ms. Peak host-process resident memory was 8634.1 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
