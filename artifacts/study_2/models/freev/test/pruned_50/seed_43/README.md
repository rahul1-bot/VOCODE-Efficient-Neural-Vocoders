# FreeV — pruned_50 — Seed 43

This execution evaluates FreeV with the `pruned_50` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 43. It measured PESQ 2.099882 and real-time factor 0.000480 on the registered B200 lane. The 520 batch-one timing observations had p50 3.132 ms and p95 3.473 ms. Peak host-process resident memory was 8676.4 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
