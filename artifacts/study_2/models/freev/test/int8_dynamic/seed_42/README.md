# FreeV — int8_dynamic — Seed 42

This execution evaluates FreeV with the `int8_dynamic` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 42. It measured PESQ 3.256816 and real-time factor 0.006675 on the registered CPU lane. The 520 batch-one timing observations had p50 45.057 ms and p95 56.615 ms. Peak host-process resident memory was 9819.2 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
