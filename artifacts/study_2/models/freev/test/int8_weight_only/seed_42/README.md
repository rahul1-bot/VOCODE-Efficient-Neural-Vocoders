# FreeV — int8_weight_only — Seed 42

This execution evaluates FreeV with the `int8_weight_only` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 42. It measured PESQ 3.377516 and real-time factor 0.000402 on the registered B200 lane. The 520 batch-one timing observations had p50 2.505 ms and p95 3.357 ms. Peak host-process resident memory was 7176.4 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
