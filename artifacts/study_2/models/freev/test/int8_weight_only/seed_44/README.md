# FreeV — int8_weight_only — Seed 44

This execution evaluates FreeV with the `int8_weight_only` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 44. It measured PESQ 3.377516 and real-time factor 0.000383 on the registered B200 lane. The 520 batch-one timing observations had p50 2.395 ms and p95 3.078 ms. Peak host-process resident memory was 7176.5 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
