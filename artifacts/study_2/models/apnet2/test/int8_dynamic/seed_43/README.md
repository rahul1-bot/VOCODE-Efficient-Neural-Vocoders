# APNet2 — int8_dynamic — Seed 43

This execution evaluates APNet2 with the `int8_dynamic` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 43. It measured PESQ 2.584164 and real-time factor 0.010880 on the registered CPU lane. The 520 batch-one timing observations had p50 73.387 ms and p95 92.988 ms. Peak host-process resident memory was 9809.1 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
