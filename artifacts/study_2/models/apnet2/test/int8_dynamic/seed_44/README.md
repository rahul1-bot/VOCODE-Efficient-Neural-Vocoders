# APNet2 — int8_dynamic — Seed 44

This execution evaluates APNet2 with the `int8_dynamic` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 44. It measured PESQ 2.584164 and real-time factor 0.011189 on the registered CPU lane. The 520 batch-one timing observations had p50 75.819 ms and p95 93.428 ms. Peak host-process resident memory was 9741.5 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
