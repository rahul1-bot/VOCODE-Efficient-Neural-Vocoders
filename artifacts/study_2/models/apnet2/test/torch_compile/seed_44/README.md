# APNet2 — torch_compile — Seed 44

This execution evaluates APNet2 with the `torch_compile` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 44. It measured PESQ 2.581322 and real-time factor 0.000428 on the registered B200 lane. The 520 batch-one timing observations had p50 2.799 ms and p95 3.373 ms. Peak host-process resident memory was 8048.6 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
