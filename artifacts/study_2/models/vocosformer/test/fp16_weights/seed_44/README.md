# VocosFormer — fp16_weights — Seed 44

This execution evaluates VocosFormer with the `fp16_weights` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 44. It measured PESQ 2.476222 and real-time factor 0.000455 on the registered B200 lane. The 520 batch-one timing observations had p50 2.080 ms and p95 7.917 ms. Peak host-process resident memory was 10541.9 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
