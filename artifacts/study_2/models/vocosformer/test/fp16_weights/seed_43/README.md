# VocosFormer — fp16_weights — Seed 43

This execution evaluates VocosFormer with the `fp16_weights` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 43. It measured PESQ 2.476222 and real-time factor 0.000500 on the registered B200 lane. The 520 batch-one timing observations had p50 2.822 ms and p95 3.886 ms. Peak host-process resident memory was 10121.6 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
