# VocosFormer — int8_dynamic — Seed 42

This execution evaluates VocosFormer with the `int8_dynamic` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 42. It measured PESQ 2.475241 and real-time factor 0.005780 on the registered CPU lane. The 520 batch-one timing observations had p50 38.723 ms and p95 50.204 ms. Peak host-process resident memory was 9838.8 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
