# VocosFormer — int8_dynamic — Seed 44

This execution evaluates VocosFormer with the `int8_dynamic` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 44. It measured PESQ 2.475241 and real-time factor 0.005313 on the registered CPU lane. The 520 batch-one timing observations had p50 36.146 ms and p95 46.007 ms. Peak host-process resident memory was 10081.0 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
