# VocosFormer — int8_dynamic — Seed 43

This execution evaluates VocosFormer with the `int8_dynamic` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 43. It measured PESQ 2.475787 and real-time factor 0.009306 on the registered CPU lane. The 520 batch-one timing observations had p50 62.686 ms and p95 80.775 ms. Peak host-process resident memory was 9663.5 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
