# VocosFormer — pruned_50 — Seed 44

This execution evaluates VocosFormer with the `pruned_50` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 44. It measured PESQ 1.591730 and real-time factor 0.000274 on the registered B200 lane. The 520 batch-one timing observations had p50 1.679 ms and p95 2.343 ms. Peak host-process resident memory was 8458.2 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
