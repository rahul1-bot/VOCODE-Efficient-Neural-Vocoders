# VocosFormer — pruned_50 — Seed 43

This execution evaluates VocosFormer with the `pruned_50` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 43. It measured PESQ 1.591730 and real-time factor 0.000263 on the registered B200 lane. The 520 batch-one timing observations had p50 1.667 ms and p95 2.148 ms. Peak host-process resident memory was 8434.3 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
