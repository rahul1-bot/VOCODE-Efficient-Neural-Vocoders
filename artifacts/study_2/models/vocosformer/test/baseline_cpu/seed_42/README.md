# VocosFormer — baseline_cpu — Seed 42

This execution evaluates VocosFormer with the `baseline_cpu` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 42. It measured PESQ 2.477956 and real-time factor 0.006616 on the registered CPU lane. The 520 batch-one timing observations had p50 44.680 ms and p95 59.353 ms. Peak host-process resident memory was 9775.2 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
