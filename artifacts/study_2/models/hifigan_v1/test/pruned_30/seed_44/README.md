# HiFi-GAN V1 — pruned_30 — Seed 44

This execution evaluates HiFi-GAN V1 with the `pruned_30` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 44. It measured PESQ 3.127416 and real-time factor 0.001102 on the registered B200 lane. The 520 batch-one timing observations had p50 6.755 ms and p95 8.584 ms. Peak host-process resident memory was 8777.0 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
