# HiFi-GAN V1 — pruned_30 — Seed 43

This execution evaluates HiFi-GAN V1 with the `pruned_30` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 43. It measured PESQ 3.127339 and real-time factor 0.001916 on the registered B200 lane. The 520 batch-one timing observations had p50 10.054 ms and p95 26.833 ms. Peak host-process resident memory was 8810.9 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
