# HiFi-GAN V1 — pruned_30 — Seed 42

This execution evaluates HiFi-GAN V1 with the `pruned_30` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 42. It measured PESQ 3.127429 and real-time factor 0.000914 on the registered B200 lane. The 520 batch-one timing observations had p50 5.900 ms and p95 7.357 ms. Peak host-process resident memory was 8850.9 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
