# HiFi-GAN V1 — baseline_cpu — Seed 44

This execution evaluates HiFi-GAN V1 with the `baseline_cpu` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 44. It measured PESQ 3.317273 and real-time factor 0.086394 on the registered CPU lane. The 520 batch-one timing observations had p50 588.757 ms and p95 858.277 ms. Peak host-process resident memory was 12967.8 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
