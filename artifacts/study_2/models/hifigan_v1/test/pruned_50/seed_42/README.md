# HiFi-GAN V1 — pruned_50 — Seed 42

This execution evaluates HiFi-GAN V1 with the `pruned_50` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 42. It measured PESQ 2.254994 and real-time factor 0.000930 on the registered B200 lane. The 520 batch-one timing observations had p50 6.029 ms and p95 7.519 ms. Peak host-process resident memory was 8773.2 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
