# HiFi-GAN V1 — torch_compile — Seed 44

This execution evaluates HiFi-GAN V1 with the `torch_compile` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 44. It measured PESQ 3.317915 and real-time factor 0.012917 on the registered B200 lane. The 520 batch-one timing observations had p50 84.512 ms and p95 119.132 ms. Peak host-process resident memory was 7524.4 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
