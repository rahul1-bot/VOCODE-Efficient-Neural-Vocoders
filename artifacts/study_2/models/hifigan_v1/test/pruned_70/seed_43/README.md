# HiFi-GAN V1 — pruned_70 — Seed 43

This execution evaluates HiFi-GAN V1 with the `pruned_70` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 43. It measured PESQ 1.304445 and real-time factor 0.000950 on the registered B200 lane. The 520 batch-one timing observations had p50 6.108 ms and p95 7.983 ms. Peak host-process resident memory was 8767.1 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
