# HiFi-GAN V1 — baseline_b200 — Seed 44

This execution evaluates HiFi-GAN V1 with the `baseline_b200` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 44. It measured PESQ 3.318409 and real-time factor 0.001253 on the registered B200 lane. The 520 batch-one timing observations had p50 6.515 ms and p95 19.654 ms. Peak host-process resident memory was 7272.6 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
