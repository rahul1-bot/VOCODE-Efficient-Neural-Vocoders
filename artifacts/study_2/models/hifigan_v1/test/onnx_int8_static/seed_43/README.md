# HiFi-GAN V1 — onnx_int8_static — Seed 43

This execution evaluates HiFi-GAN V1 with the `onnx_int8_static` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 43. It measured PESQ 2.053486 and real-time factor 0.083621 on the registered CPU lane. The 520 batch-one timing observations had p50 567.155 ms and p95 801.686 ms. Peak host-process resident memory was 16889.3 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
