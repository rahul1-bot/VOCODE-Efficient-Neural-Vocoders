# HiFi-GAN V1 — onnx_int8_static — Seed 44

This execution evaluates HiFi-GAN V1 with the `onnx_int8_static` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 44. It measured PESQ 1.909917 and real-time factor 0.107213 on the registered CPU lane. The 520 batch-one timing observations had p50 731.182 ms and p95 1012.179 ms. Peak host-process resident memory was 16277.4 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
