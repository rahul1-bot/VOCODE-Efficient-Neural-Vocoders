# RFWave — baseline_b200 — Seed 44

This execution evaluates RFWave with the `baseline_b200` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 44. It measured PESQ 3.777031 and real-time factor 0.009319 on the registered B200 lane. The 520 batch-one timing observations had p50 63.775 ms and p95 80.340 ms. Peak host-process resident memory was 6978.5 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
