# RFWave — fp16_weights — Seed 44

This execution evaluates RFWave with the `fp16_weights` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 44. It measured PESQ 3.776786 and real-time factor 0.004357 on the registered B200 lane. The 520 batch-one timing observations had p50 28.668 ms and p95 37.367 ms. Peak host-process resident memory was 11619.2 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
