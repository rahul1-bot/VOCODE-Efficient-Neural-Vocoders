# RFWave — fp16_weights — Seed 43

This execution evaluates RFWave with the `fp16_weights` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 43. It measured PESQ 3.782269 and real-time factor 0.005205 on the registered B200 lane. The 520 batch-one timing observations had p50 33.444 ms and p95 41.112 ms. Peak host-process resident memory was 11563.0 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
