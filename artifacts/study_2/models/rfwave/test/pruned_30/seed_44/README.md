# RFWave — pruned_30 — Seed 44

This execution evaluates RFWave with the `pruned_30` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 44. It measured PESQ 1.040598 and real-time factor 0.008988 on the registered B200 lane. The 520 batch-one timing observations had p50 60.810 ms and p95 79.433 ms. Peak host-process resident memory was 8578.0 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
