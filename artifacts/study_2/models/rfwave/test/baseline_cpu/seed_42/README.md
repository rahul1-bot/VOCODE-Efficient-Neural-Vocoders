# RFWave — baseline_cpu — Seed 42

This execution evaluates RFWave with the `baseline_cpu` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 42. It measured PESQ 3.784488 and real-time factor 0.607145 on the registered CPU lane. The 520 batch-one timing observations had p50 3909.354 ms and p95 6138.015 ms. Peak host-process resident memory was 12651.4 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
