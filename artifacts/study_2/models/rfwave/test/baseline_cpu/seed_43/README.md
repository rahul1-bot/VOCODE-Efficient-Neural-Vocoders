# RFWave — baseline_cpu — Seed 43

This execution evaluates RFWave with the `baseline_cpu` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 43. It measured PESQ 3.782729 and real-time factor 0.585386 on the registered CPU lane. The 520 batch-one timing observations had p50 3770.293 ms and p95 5857.549 ms. Peak host-process resident memory was 12444.7 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
