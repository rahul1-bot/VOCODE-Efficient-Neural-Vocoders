# RFWave — baseline_cpu — Seed 44

This execution evaluates RFWave with the `baseline_cpu` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 44. It measured PESQ 3.787563 and real-time factor 0.577137 on the registered CPU lane. The 520 batch-one timing observations had p50 3679.062 ms and p95 5800.263 ms. Peak host-process resident memory was 12419.7 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
