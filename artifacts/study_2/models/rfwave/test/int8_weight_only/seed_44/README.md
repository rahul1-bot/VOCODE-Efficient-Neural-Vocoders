# RFWave — int8_weight_only — Seed 44

This execution evaluates RFWave with the `int8_weight_only` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 44. It measured PESQ 3.775521 and real-time factor 0.008939 on the registered B200 lane. The 520 batch-one timing observations had p50 54.790 ms and p95 80.144 ms. Peak host-process resident memory was 6997.8 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
