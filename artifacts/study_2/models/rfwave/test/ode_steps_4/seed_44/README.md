# RFWave — ode_steps_4 — Seed 44

This execution evaluates RFWave with the `ode_steps_4` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 44. It measured PESQ 3.412283 and real-time factor 0.004902 on the registered B200 lane. The 520 batch-one timing observations had p50 29.941 ms and p95 48.856 ms. Peak host-process resident memory was 8548.8 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
