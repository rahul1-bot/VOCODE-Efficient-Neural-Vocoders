# RFWave — int8_dynamic — Seed 44

This execution evaluates RFWave with the `int8_dynamic` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 44. It measured PESQ 3.141335 and real-time factor 0.412313 on the registered CPU lane. The 520 batch-one timing observations had p50 2518.732 ms and p95 4306.595 ms. Peak host-process resident memory was 11447.7 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
