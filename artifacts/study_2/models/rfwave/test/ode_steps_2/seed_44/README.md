# RFWave — ode_steps_2 — Seed 44

This execution evaluates RFWave with the `ode_steps_2` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 44. It measured PESQ 2.145749 and real-time factor 0.001947 on the registered B200 lane. The 520 batch-one timing observations had p50 12.965 ms and p95 18.212 ms. Peak host-process resident memory was 8567.9 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
