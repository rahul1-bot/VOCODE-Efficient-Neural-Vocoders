# RFWave — ode_steps_2 — Seed 42

This execution evaluates RFWave with the `ode_steps_2` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 42. It measured PESQ 2.144336 and real-time factor 0.002141 on the registered B200 lane. The 520 batch-one timing observations had p50 13.155 ms and p95 21.847 ms. Peak host-process resident memory was 8582.8 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
