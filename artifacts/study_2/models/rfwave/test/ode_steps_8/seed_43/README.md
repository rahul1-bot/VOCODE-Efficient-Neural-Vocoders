# RFWave — ode_steps_8 — Seed 43

This execution evaluates RFWave with the `ode_steps_8` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 43. It measured PESQ 3.736667 and real-time factor 0.007356 on the registered B200 lane. The 520 batch-one timing observations had p50 50.586 ms and p95 63.582 ms. Peak host-process resident memory was 8542.4 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
