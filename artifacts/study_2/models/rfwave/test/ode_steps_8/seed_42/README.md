# RFWave — ode_steps_8 — Seed 42

This execution evaluates RFWave with the `ode_steps_8` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 42. It measured PESQ 3.735628 and real-time factor 0.007717 on the registered B200 lane. The 520 batch-one timing observations had p50 51.924 ms and p95 68.574 ms. Peak host-process resident memory was 8543.6 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
