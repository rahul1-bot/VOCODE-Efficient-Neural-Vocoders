# RFWave — ode_steps_8 — Seed 44

This execution evaluates RFWave with the `ode_steps_8` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 44. It measured PESQ 3.731822 and real-time factor 0.007645 on the registered B200 lane. The 520 batch-one timing observations had p50 51.819 ms and p95 67.528 ms. Peak host-process resident memory was 8556.2 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
