# RFWave — pruned_50 — Seed 43

This execution evaluates RFWave with the `pruned_50` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 43. It measured PESQ 1.055138 and real-time factor 0.009262 on the registered B200 lane. The 520 batch-one timing observations had p50 64.190 ms and p95 79.561 ms. Peak host-process resident memory was 8544.2 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
