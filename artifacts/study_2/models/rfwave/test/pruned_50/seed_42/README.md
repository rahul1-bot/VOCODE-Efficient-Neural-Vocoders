# RFWave — pruned_50 — Seed 42

This execution evaluates RFWave with the `pruned_50` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 42. It measured PESQ 1.063550 and real-time factor 0.010080 on the registered B200 lane. The 520 batch-one timing observations had p50 68.227 ms and p95 83.695 ms. Peak host-process resident memory was 8560.5 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
