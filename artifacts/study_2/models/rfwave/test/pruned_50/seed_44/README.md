# RFWave — pruned_50 — Seed 44

This execution evaluates RFWave with the `pruned_50` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 44. It measured PESQ 1.065988 and real-time factor 0.010359 on the registered B200 lane. The 520 batch-one timing observations had p50 66.121 ms and p95 104.011 ms. Peak host-process resident memory was 8551.2 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
