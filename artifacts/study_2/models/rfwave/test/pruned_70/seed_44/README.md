# RFWave — pruned_70 — Seed 44

This execution evaluates RFWave with the `pruned_70` configuration on all 525 adaptive-benchmark LJSpeech utterances using evaluation seed 44. It measured PESQ 1.059203 and real-time factor 0.008659 on the registered B200 lane. The 520 batch-one timing observations had p50 60.246 ms and p95 76.060 ms. Peak host-process resident memory was 8577.0 MiB.

`config.yaml` defines the executed data, intervention, and timing conditions. `execution.log` records the run events, and `metrics.csv` contains the complete Study 2 measurement row.
