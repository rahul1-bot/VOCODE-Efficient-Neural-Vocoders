# Study 2 Figures

`intervention_landscape` maps same-lane speedup against PESQ change for all non-denominator groups. `classification_matrix` reports model-level outcome counts. `technique_summary` compares median effects by variant. PNG files provide high-resolution previews and PDF files preserve vector geometry.
