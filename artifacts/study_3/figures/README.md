# Study 3 Figures

The Pareto figures report true-length PESQ against B200 real-time factor, parameter count, and serialized size. `deployment_hardware` compares isolated B200, M3 Max, and 8-core CPU timing. `intervention_landscape` connects the baseline synthesis to the included intervention evidence. LPCNet is visibly marked as a 27-utterance, one-seed PyTorch timing exception and is excluded from the strict speed frontier. PNG files provide high-resolution previews and PDF files preserve vector geometry.
