# RFWave Test — Seed 44

Complete 525-utterance B200 FP32 evaluation of the retained epoch-615, step-120,000 checkpoint. `config.yaml` fixes the protocol, `execution.log` preserves the source execution trace, and `metrics.csv` records the Study 1 measurements.
